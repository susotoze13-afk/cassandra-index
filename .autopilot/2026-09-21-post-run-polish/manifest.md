# Манифест: Доводка замечаний + публикация

Прогон-маленький (T1), режим full, глубина normal. Пользователь выбрал два
направления: «Публикуй» (push разрешён, выполняется одним пакетом в конце
прогона) и «Доводка замечаний» (закрыть мелкие замечания ревью прогона
`2026-09-21-index-3weeks-calibration`).

## Требования

| ID | Требование | Приёмка |
|---|---|---|
| R01 | Опубликовать накопленное: push в master, автоматический выкат GitHub Actions, проверка живого URL | `git push origin master` выполнен; `https://susotoze13-afk.github.io/cassandra-index/data/2026-09-13/global.js` отдаёт `"index":57`; хвост trend 60/58/57 |
| R02 | Доводка замечаний ревью предыдущего прогона — позиции 1–15 ниже | Каждая позиция закрыта кодом/текстом; `node --test` зелёный |
| R02i | Счётчики тестов в AGENTS.md соответствуют факту после правок | Число тестов и файлов в AGENTS.md = фактический прогон |

## R02 — перечень замечаний (источник: concerns прогона 2026-09-21-index-3weeks-calibration)

Таск A — код и тесты (`calc/`, `tests/`):

1. `tests/calibrate.test.js`: нет теста на tie-break `selectK` (равный
   minMargin → меньший k; именно эта ветка выбрала k=1.95). Добавить тест,
   способный быть красным.
2. `calc/calibrate.js` main(): minMargin/allHit вычисляются заново вместо
   переиспользования общей логики попадания по строкам evaluation — свести
   в одно место.
3. `calc/calc.js`: `totalCriteria: 45` захардкожен — взять из
   `Object.keys(CRITERIA).length` (engine).
4. `calc/calc.js` runWeek: вручную воспроизведена ветка слепого региона
   вместо вызова `engine.regionalIndex` — направить через engine даже в
   вырожденном случае (шов на будущее).
5. `calc/calc.js` writeWeek: файлы пишутся ДО самопроверки validateSnapshot —
   самопроверка до записи (или откат).
6. `calc/engine.js`: имя `round1` против Math.round (округление до целых) —
   переименовать.
7. `calc/engine.js`: мёртвая ветка `signaling.length >= 3` для Д9.6b
   (недостижима) — убрать или сделать достижимой.
8. `calc/engine.js` d8Strength: избыточная проверка `covered.length === 0` —
   упростить.
9. `tests/calc-engine.test.js`: устаревший комментарий «round(73.10585786…)»
   (это k=2.0; при k=1.95 верно 72.61, ассерт 73 верен) — поправить
   комментарий.

Таск B — журнал и якорные профили (`docs/calibration-journal.md`,
`calc/input/anchors/cuban-1962.json`):

10. Журнал: столбец I_agg proxy-sanctions-2018 (0.132) не бьётся с индексом
    32 (≈0.163) — исправить цифру или дать расшифровку.
11. Журнал: «запас» verify-якорей посчитан как index−lo, а не
    min(index−lo, hi−index) (Карибский «2» вместо 1, фев-2022 «10» вместо
    5) — привести к формуле marginOf из calibrate.js.
12. Журнал: «без лимита ~99.9, с лимитом ~99.4» не сходится с прогоном
    (~99.8 / ~98.8) — исправить промежуточные числа.
13. Журнал: явная фраза о намеренном нарушении строгой хронологии
    rolling-origin для verify Карибского-1962 (защита от подгонки k под
    верхний кластер).
14. Журнал: зафиксировать правило «Д9 medium при полностью пустом Д9
    занижает 1−q (c=0.7 в знаменателе q)» — оговорка для будущих входов.
15. `calc/input/anchors/cuban-1962.json`: D5.1–D5.3 = 1 для 1962 при
    отсутствии киберсферы — добавить в файл/journal явную трактовку
    «D5 = инфо-измерение эпохи».

## Допущения (режим full, брифинг пропущен — прогон маленький)

- Список замечаний фиксирован: «не чинить» из concerns (trend-хвосты старых
  недель, narrative-поля регионов) — наследие демо-данных, оставляем.
- `js/**` и `data/**` не трогаем — сайт и снапшоты не меняются, бандл
  пересобирать не нужно.
- Число тестов после правок вырастет — точное новое число определит прогон
  (R02i).
- Один коммит на таск, финальный коммит прогона; push — после приёмки,
  одним пакетом (R01).

## Итоги

| ID | Статус | Что стало |
|---|---|---|
| R01 | done | Push после доводки одним пакетом; живой URL проверен curl |
| R02 | done | Пункты 1–15 закрыты (таск A: 2a22308, таск B: df9314e); оба craft-ревью clean; числа не изменились (60/58/57, k=1.95) |
| R02i | done | AGENTS.md: 151 passed / 0 fail (было 150) |

## Отклонённые направления

- Расширение покрытия критериев и доработка демо-данных — за пределами
  прогона (D01: веб по окнам 2026 пуст, выдумывать источники запрещено).
- Ручная правка опубликованных значений обратно на 63/66/72 — запрещено
  правилами прозрачности (честный эффект низкого покрытия).
