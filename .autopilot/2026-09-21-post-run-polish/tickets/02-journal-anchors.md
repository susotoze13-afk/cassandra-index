# Таск 02 (B): Журнал калибровки и якорь cuban-1962

Зона: `docs/calibration-journal.md`, `calc/input/anchors/cuban-1962.json`.
Код и тесты НЕ трогаем.

Пункты 10–15 из спецификации (`spec.md`, раздел «Таск B»):

- I_agg proxy-sanctions-2018: 0.132 → фактическое число расчёта или
  расшифровка шагов.
- «Запас» verify-якорей по min(index−lo, hi−index): Карибский 1962 → 1,
  фев-2022 → 5 (формула marginOf из calc/calibrate.js).
- Sensitivity: промежуточные «~99.9 / ~99.4» → фактические ~99.8 / ~98.8.
- Явная фраза про намеренное нарушение строгой хронологии rolling-origin
  для verify Карибского-1962.
- Оговорка: Д9 medium при полностью пустом Д9 занижает 1−q (c=0.7 в
  знаменателе q).
- cuban-1962.json: трактовка «D5 = инфо-измерение эпохи» для D5.1–D5.3=1
  в 1962 (в файле допустимым JSON-форматом снапшота и/или в журнале).

Формат входа якоря должен остаться валидным для calc/ (JSON.parse +
валидатор engine). Проверить, что `node calc/calc.js` и калибровка
по-прежнему отрабатывают на якорях (как smoke, без изменения чисел).

Не трогать: `js/**`, `data/**`, код calc/, тесты.
