# Спецификация: Доводка замечаний + публикация

Режим full, прогон T1. Брифинг не проводился (маленький прогон) — допущения
в `manifest.md`. Требования: R01 (публикация), R02 (доводка, позиции 1–15),
R02i (счётчики тестов в AGENTS.md).

## Таск A — код и тесты calc/

Зона: `calc/calibrate.js`, `calc/engine.js`, `calc/calc.js`,
`tests/calibrate.test.js`, `tests/calc-engine.test.js`.

1. **Тест tie-break selectK.** В `tests/calibrate.test.js` добавить тест:
   два якорных профиля с одинаковым minMargin у разных k — выбирается меньший
   k. Тест обязан падать, если tie-break убрать/инвертировать (проверить
   красным до правки логики не требуется — ветка уже существует; тест
   фиксирует её).
2. **Общая логика попадания.** В `calc/calibrate.js` main() minMargin/allHit
   сейчас вычисляются заново, а не переиспользуют логику оценки строк
   evaluation — свести определение «попадание в окно» в одну функцию,
   используемую и в evaluation, и в итоговом выводе.
3. **totalCriteria из engine.** В `calc/calc.js` заменить хардкод 45 на
   `Object.keys(engine.CRITERIA).length` (или экспортированный размер).
4. **runWeek через engine.regionalIndex.** В `calc/calc.js` runWeek не
   вызывает `engine.regionalIndex`, а вручную воспроизводит ветку слепого
   региона (nReg=0 → background) — направить через engine даже в
   вырожденном случае.
5. **Самопроверка до записи.** В `calc/calc.js` writeWeek пишет файлы до
   validateSnapshot — переставить: собрать снапшот, проверить, потом писать.
6. **Переименование round1.** В `calc/engine.js` round1 → имя, не
   конфликтующее с семантикой Math.round (округление до целых с
   полушагом/банкирским — как фактически реализовано; имя должно это
   отражать).
7. **Мёртвая ветка Д9.6b.** В `calc/engine.js` ветка `signaling.length >= 3`
   недостижима — убрать или сделать достижимой по формулировке
   METHODOLOGY.md; изменение не должно менять результаты текущих недель
   (покрытие Д9 = 1 наблюдение, ветка не срабатывает — зафиксировать это
   тестом).
8. **d8Strength.** Убрать избыточную проверку `covered.length === 0`.
9. **Комментарий в calc-engine.test.js.** Поправить устаревший комментарий
   «round(73.10585786…)» — при k=1.95 верно 72.61, ассерт 73 верен.

Ограничения: результаты пересчёта трёх недель (60/58/57) и якорей НЕ
меняются; меняется только качество кода и тестов. Если правка 4 или 7
меняет числа — стоп и вернуться с объяснением.

## Таск B — журнал и якорные профили

Зона: `docs/calibration-journal.md`, `calc/input/anchors/cuban-1962.json`.

10. I_agg proxy-sanctions-2018: цифра 0.132 не бьётся с индексом 32
    (≈0.163) — исправить число по фактическому расчёту или добавить
    расшифровку промежуточных шагов.
11. «Запас» verify-якорей: пересчитать по min(index−lo, hi−index)
    (формула marginOf из calibrate.js): Карибский 1962 → 1, фев-2022 → 5.
12. Промежуточные числа sensitivity: «без лимита ~99.9 / с лимитом ~99.4»
    → фактические ~99.8 / ~98.8 по прогону.
13. Явная фраза: verify Карибского-1962 намеренно нарушает строгую
    хронологию rolling-origin (защита от подгонки k под верхний кластер).
14. Оговорка: Д9 medium при полностью пустом Д9 занижает 1−q (c=0.7 в
    знаменателе q) — правило для будущих входов.
15. cuban-1962.json: D5.1–D5.3 = 1 при отсутствии киберсферы — трактовка
    «D5 = инфо-измерение эпохи» в файле (поле трактовки/комментарий,
    допустимый форматом JSON снапшота) и/или в журнале.

Ограничения: `data/**` и `js/**` не трогать; код не менять (таск B — только
документы и якорный вход).

## Волны

- Волна 1: таск A и таск B параллельно (зоны не пересекаются; только A
  трогает tests/).
- Затем: прогон `node --test`, craft-ревью, коммит на таск, R02i, финал,
  push (R01), проверка живого URL.

## Тесты

- Команда: `node --test` (из корня, без аргументов; форма `node --test
  tests/` на Windows падает — не использовать). Один файл: `node --test
  tests/<имя>.test.js`.
- До: 150 passed / 0 fail. После таска A: число вырастет (новые тесты) —
  фиксируем факт и обновляем AGENTS.md (R02i).
