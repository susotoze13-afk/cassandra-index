# 11 — Консолидация дублей, усиление тестов, навигация на 360px

**Требования:** R04, R16, R45, R49, R80 (закрыты; таск укрепляет их реализацию)
**Blocked by:** —
**Зона:** `js/**` (кроме создаваемого таском 10 bundle), `css/styles.css`, `privacy.html`, `tests/`
**Волна:** 9
**Status:** ready

## Почему таск существует

Триаж concerns после ревью 01–09 (фаза 8 §1a): одна и та же находка —
рассеянные копии мелких хелперов — повторилась в пяти+ тасках. Правило прогона:
повтор через три таска и более → «fix now».

## Что сделать

1. **Общие хелперы в одном месте** (новый `js/ui.js`, экспорты импортируются остальными):
   - `el()` — сейчас копии в `js/sections/drivers.js:58` и `js/sections/trend.js:73`;
   - маппинг класса дельты — третья копия `js/sections/history.js:179` рядом с
     `DELTA_CLASS` в hero.js:48 и regions.js:46; класс должен выводиться из
     `risk.deltaTone`, а не пересчитываться новым каскадом по знаку;
   - `resolveLang` — `js/app.js` экспортирует, но `js/share.js:162-169` и
     `privacy.html:61-68` держат свои копии (localStorage + navigator.language);
   - разбор `?week=` — `js/app.js` владелец, но копии в `js/share.js:180-186` и
     `js/sections/history.js:101-126`.
2. **Оркестрация смены недели — в app.js**: `js/sections/history.js` сам пересобирает
   appState из `data.week()` и ведёт URL; секция должна только инициировать событие,
   владелец состояния и URL — app.js.
3. **Общий builder accordion-источников**: разметка продублирована между
   `js/sections/drivers.js` и `js/sections/regions.js` (копии уже разошлись по
   `moreBtn.hidden`); собрать в общий экспорт (теперь править buildSourceItem можно).
4. **initShare — гард на уровне модуля** (`js/share.js:244-253`): слушатель на document,
   гард на dataset кнопки — при пересоздании кнопки повесится второй слушатель.
5. **Методология из risk.SCALE**: `js/sections/methodology.js:22-29` зашивает диапазоны
   порогов в `ANCHOR_KEYS`; risk.js — единственное место порогов, таблица должна
   выводиться из `risk.SCALE`.
6. **Мёртвые i18n-ключи**: `sources.count` (i18n.js:53) и `state.historical` (i18n.js:55)
   объявлены, но кодом не читаются — удалить (проверь grep'ом по js/, что не читаются).
7. **Навигация на 360px**: `.site-nav` — горизонтальный скролл (scrollWidth 378 > 328),
   «Методология»/«История» обрезаны до свайпа. Требование — reflow без горизонтального
   скролла. Решение за тобой (перенос строкой / компактнее), главное — все 5 пунктов
   видны без свайпа на 360 и 320, десктоп не пострадал.
8. **Усилить тесты**:
   - `tests/privacy.test.js:18-24` — EN-проверки §17/footer.ip слабые (length/includes);
     сверить EN-тексты дословно, как RU;
   - `tests/privacy.test.js:34-43` — проверки наличия подстрок; страница-заглушка
     пройдёт — проверять смысловое содержимое (текст рядом с ключом описывает цель
     и хранилище);
   - `tests/history.test.js:54-66` — DEMO_REVIEWS проверяет тип/длину; добавить
     проверку, что ru и en различны и непусты.

## Критерии приёмки

- [ ] Один экземпляр каждого хелпера; grep по js/ не находит копий
- [ ] Смена недели — оркестрация в app.js; history.js и share.js переиспользуют
- [ ] Аккордеон источников — один builder на drivers и regions
- [ ] initShare повторный вызов не дублирует слушатель (тест)
- [ ] Таблица методологии выводится из risk.SCALE (тест на соответствие диапазонов)
- [ ] Мёртвые ключи удалены; навигация на 360 и 320 без горизонтального скролла
- [ ] `node --test` зелёный; счёт тестов вырос; сайт по-прежнему работает с http
