# Бриф — 2026-09-20

> Собери Cassandra Index по спецификации `PRD_Casandra_Index.md` (v1.3) в этом репозитории.
> В репозитории также лежит ранний прототип `cassandra-index-prototype.html` — используй его
> как отправную точку / референс.

Источник диктовки: пользователь активировал `/autopilot` в репозитории, содержащем
PRD v1.3 и прототип. Текст PRD (`PRD_Casandra_Index.md`, 952 строки) является
диктованной спецификацией продукта и входит в манифест требований как есть.
Прототип (`cassandra-index-prototype.html`) — существующая ранняя реализация,
предшествующая PRD v1.3.

## Дополнения

- 2026-09-20 — брифинг, ответы пользователя: (1) «Весь сайт по PRD» — собираются все разделы,
  крит-режим, состояния данных, предыдущие недели, share-карточка, политика приватности;
  (2) «Имитация по часовому поясу» — первый визит показывает регион по часовому поясу браузера,
  не сохраняя его в localStorage; реальный IP-детект не реализуется (нужен бэкенд и провайдер);
  (3) «Прототипа и PRD достаточно» — эталоном служат cassandra-index-prototype.html и нормативная
  палитра/токены PRD, отдельных референсов пользователь не назвал.
- 2026-09-20 — «разработай дизайн макет в pencil dev» — новое требование посреди прогона:
  дизайн-макет сайта в инструменте pen.dev (pencil dev).
