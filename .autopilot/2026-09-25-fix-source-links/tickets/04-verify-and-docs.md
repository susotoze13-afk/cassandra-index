# 04 — Верификация прогона: ссылки, тесты, бандлы, память

**Требования:** R05i
**Blocked by:** 02, 03
**Зона:** `js/bundle*.js`, `AGENTS.md`
**Волна:** 3
**Status:** ready

## Что должно заработать

Прогон закрыт целиком: все опубликованные ссылки проверены боевой командой
`node calc/check-sources.js` (код 0), весь набор тестов зелёный, бандлы
собраны актуальными, память проекта (AGENTS.md) знает про новую команду
проверки ссылок.

## Из брифа, дословно

> «Необходимо проверять работоспособность ссылок-источников»

## Разделы спецификации

Решения §4–5, Истории 7–8.

## Критерии приёмки

- [ ] `node calc/check-sources.js` — все недели, все URL, код выхода 0
- [ ] `node --test` (весь набор, без аргументов) зелёный
- [ ] Если `js/**` менялся в этом прогоне — `node build.js` выполнен и бандлы коммитоготовы (в прогоне код сайта не меняется — тогда бандлы не трогать)
- [ ] AGENTS.md: в разделе команд появилась строка про `node calc/check-sources.js`
- [ ] Демо-данные остаются демо: правки только в URL/заголовках/датах источников, числа индекса не изменились
