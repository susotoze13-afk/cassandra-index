# Манифест требований

Источник: `2026-09-25-brief.md`. Строку из этого списка может снять **только пользователь**.

| ID | Из брифа (дословно) | Статус | Основание | Где |
|----|---------------------|--------|-----------|-----|
| R01 | «Все ссылки на источники битые - We can't find that page или Page not found!» | done | — | spec §1 → T03 |
| R02 | «исправить битые ссылки» | done | — | spec §2 → T03 |
| R03 | «Необходимо проверять работоспособность ссылок-источников» | done | — | spec §3–5 → T01, T02 |
| R04i | *(подразумевается)* ссылки-источники — это данные снапшотов `data/<неделя>/sources.js`, показываемые в секции «Источники» сайта | done | — | spec §1 → T03 |
| R05i | *(подразумевается)* замена URL не должна ломать контракт данных (`validate` из js/data.js) и тесты | done | — | spec §6 → T04 (+T03) |
| R06i | *(подразумевается)* «проверять работоспособность» — повторяемая проверка (в пайплайне расчёта недели), а не разовая ручная | done | — | spec §4 → T02 |
| D01 | *(доказано сборкой)* apnews.com и crisisgroup.org отдают 403 (Cloudflare JS-challenge) на любые bot-запросы из этой среды; полные тексты статей при этом получаются серверным fetch'ем — 403 не может считаться «битой ссылкой» | in-spec | спека §3 изменена: classify → ok/blocked/broken, ворота падают только по broken | spec §3 |
| D02 | *(доказано сборкой)* DNS среды не резолвит www.bbc.com и www.dw.com (страницы живы — 200 через --resolve); reliefweb.int отдаёт 406 на дефолтный fetch-UA — «ответ не получен» и «сервер отверг клиента» ≠ «страницы нет» | in-spec | спека §3 изменена: classify(null) → blocked, запросы с браузерными заголовками | spec §3 |
| D03 | *(доказано сборкой)* reliefweb.int отдаёт интермитентный 406 (бот-фильтр: те же URL «ок»/202 в одних прогонах и 406 в других, флап на минутах) — финальный 406 ≠ «страницы нет» | in-spec | спека §3 изменена: 406 ретраится, classify(406) → blocked | spec §3 → T06 |
