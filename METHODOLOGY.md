# Cassandra Index — Методология расчёта

Статус: рабочий каркас методологии, соответствующий `PRD_Casandra_Index.md` v1.3.
Версия документа: **0.5** — второй раунд правок по результатам внешнего ревью:
формула скора драйвера как разность нормировок (устранены искажение знаменателя
α и двусмысленность знака), степенное сжатие вместо неопределённой сигмоиды,
правило зеркалирования глобального шока в регионах, «2 независимых ИЛИ 1
верифицированный primary/OSINT», генеалогия источников и кластеры происхождения,
абсолютный потолок вклада Д9 (25 %), разделение критериев Д9 на циклические и
ациклические, критерий Д8.4 «снижение интенсивности», перцентильная
интерпретация шкалы, механизм Flash Alert, исторические якоря порогов,
асимметричная инерция.

---

## 1. Назначение и границы

**Cassandra Index** — нормированная шкала состояния риска глобального конфликта
от 0 до 100, рассчитываемая еженедельно на основе открытых данных.

Жёсткие границы (нормативны, из PRD):

- Индекс **не является вероятностью войны** 0–100 % и никогда так не
  интерпретируется (PRD §2.2).
- Индекс **не предсказывает дату** начала конфликта; метафора обратного
  отсчёта исключена (PRD §2.1, §15).
- Модельная оценка **не равна официальному объявлению ЧС** (PRD §2.4).
- Избегается ложная точность: никаких десятичных значений, узких диапазонов
  и необоснованной определённости (PRD §2.3). Все публикуемые значения —
  целые числа; расчёт внутри пайплайна ведётся в float (§5.4).
- Внутренние скоринги (Brier, z-score и т.п.) остаются в закрытой части
  методологии и не показываются пользователю (PRD §8.1, §11.5).

### 1.1. Интерпретация шкалы (решение открытого вопроса PRD §19.1)

Шкала интерпретируется как **перцентиль аномальности (Anomaly Percentile)**,
а не как вероятность события:

> Значение индекса отражает, насколько текущая комбинация сигналов
> отклоняется от базовой линии мирного времени (ориентир — 2010–2019 гг.).
> Индекс 70 означает, что напряжение соответствует 70-му перцентилю
> исторически зафиксированных кризисов: в истории были кризисы такого же
> накала, не переросшие в войну.

Эта формулировка снимает вопрос «почему индекс 80, а войны нет» и задаёт
базовую линию для калибровки (§5.2, §9).

---

## 2. Недельный цикл расчёта

Расчёт — батчевый, не реального времени (PRD §9):

| Этап | Период |
|---|---|
| Окно сбора данных | Понедельник 00:00 – воскресенье 24:00 UTC |
| Расчёт и редакторская проверка | Понедельник–вторник |
| Публикация | Вторник 12:00 UTC |

Результат каждой недели — **неизменяемый снапшот**:

```
/data/YYYY-MM-DD/global.json
/data/YYYY-MM-DD/regions.json
/data/YYYY-MM-DD/region-<id>.json
/data/YYYY-MM-DD/trend.json
/data/YYYY-MM-DD/drivers.json
/data/YYYY-MM-DD/sources.json
```

Снапшот привязан к дате покрытия данных (`Data through`), которая в UI
никогда не сливается с датой публикации (`Last published`).

Событие, произошедшее между недельными релизами, не меняет опубликованный
снапшот; оно учитывается в следующем окне. Для событий-триггеров между
релизами действует механизм Flash Alert (§2.1).

### 2.1. Flash Alert (решение открытого вопроса PRD §19.5)

Если между релизами происходит событие, подпадающее под критерии **Д1.4**
(начало активных боевых действий нового масштаба, новый театр) или **Д2.4**
(публичное повышение готовности ядерных сил), публикуется **внеплановый
алерт**, а не пересчёт индекса:

- алерт содержит цепочку доказательств (Observation → Why it matters →
  Confidence → Sources, §3.2) и явную пометку «Flash Alert — вне цикла
  недельного снапшота»;
- опубликованный индекс **не изменяется** до ближайшего вторника — это
  защищает еженедельный ритм и сопоставимость рядов;
- событие полностью учитывается в расчёте следующего окна.

Таким образом сервис не «молчит» до вторника, но и не разрушает
воспроизводимость снапшотов спонтанными пересчётами.

---

## 3. Общая структура расчёта

Пайплайн многоуровневый:

```
Источники → Наблюдения → Критерии → Драйверы → Глобальный индекс → Региональные индексы → Состояние и изменение
```

### 3.1. Источники

- Минимум один источник на драйвер; рекомендуется 2–5 (PRD §4.3.1).
- Если источников больше пяти — публикуются первые пять + ссылка «показать все».
- **Независимость источников проверяется через генеалогию и кластеры
  происхождения** (§4.1.3): перепечатка материала агентства тем же
  агентством, анализ OSINT-сообщества того же события — один
  независимый источник, а не два (критерий снижения уверенности, PRD §8.1).
- Каждый источник несёт обязательные поля: заголовок, домен, дата, URL,
  тип (primary / secondary / OSINT) — минимальный набор полей, PRD §19.17.
- Спекулятивные индикаторы не входят в расчёт как установленные факты
  (PRD §4.3).

### 3.2. Наблюдения (Observation)

Каждое наблюдение:

- относится к одному критерию (индикатору, см. §4) и через него — к драйверу;
- привязано к дате внутри недельного окна;
- содержит ссылку на источник(и);
- формулируется одним предложением на человеческом языке (это же предложение
  уходит в UI драйвера, PRD §4.3).

Цепочка доказательств по каждому драйверу (PRD §8.2):

```
Observation → Why it matters → Confidence → Sources
```

### 3.3. Драйверы

Драйвер — факторная группа, объединяющая несколько критериев (§4).
Полный список драйверов и их веса фиксируются в версии методологии.

Каждый драйвер в расчёте несёт три внутренних параметра:

| Параметр | Тип | Назначение |
|---|---|---|
| Скор `sᵢ` | [0, 1] или `null` | Нетто-сигнал эскалации драйвера за неделю (§4.2) |
| Вес `wᵢ` | [0, 1], Σwᵢ = 1 | Стартовый вклад драйвера, перенормировывается с учётом уверенности (§5.1) |
| Уверенность `cᵢ` | {высокая 1.0, средняя 0.7, низкая 0.4} | **Модификатор веса**, а не множитель сигнала: снижает влияние драйвера при плохих данных, не занижая масштаб индекса (§5.1) |

Для чисто деэскалационного Д8 вместо `s₈` считается сила деэскалации `d₈`
(§4.2) — она вычитается из сырого скора на этапе агрегации (§5).

Пользователю показываются только качественные уровни: вклад — высокий /
средний / низкий; уверенность — высокая / средняя / низкая (PRD §4.3, §8.1).

---

## 4. Полный перечень критериев расчёта

Критерий (индикатор) — минимальная учитываемая единица сигнала. Каждый
критерий принадлежит ровно одному драйверу. Состав: **9 драйверов,
44 критерия** (Д1–Д8 — прямые сигналы, Д9 — косвенные индикаторы
подготовки, агрегированные иерархически по подгруппам, §4.3).
Веса и состав — до калибровки на исторических данных.

Обозначения в таблицах критериев:

- **Направление** — `эск.` вносит вклад в рост индекса, `деэск.` — в снижение;
- **Шкала** — как кодируется сигнал критерия за неделю:
  - `бинарная` — событие произошло/нет (0/1);
  - `порядковая 0–3` — нет / единичные / регулярные / массовые проявления;
  - `счётная (CAP 5)` — количество подтверждённых событий, сверху ограничено
    значением 5, чтобы одно крупное событие не доминировало в расчёте;
- **Мин. источников** — минимум **2 независимых источника из разных
  кластеров происхождения** (§4.1.3) **ИЛИ 1 верифицированный
  primary/OSINT** (§4.1.2). Ниже — критерий считается **непокрытым**:
  исключается из знаменателя скора, а не засчитывается как ноль (§4.2).

### Д1. Военная активность (Military activity)

Сюда входят только независимо подтверждённые признаки: спутниковые снимки,
официальные объявления, проверенные OSINT-группы. Слухи и неподтверждённые
сообщения не учитываются.

| # | Критерий | Направление | Шкала | Мин. источников |
|---|---|---|---|---|
| Д1.1 | Перемещения войск и военных колонн (наземные, по железной дороге, морские переброски) вблизи театра или границ | эск. | счётная (CAP 5) | 2 |
| Д1.2 | Анонсированные и фактические учения необычного масштаба, близости к границе или одновременности фронтов | эск. | порядковая 0–3 | 2 |
| Д1.3 | Наращивание военной логистики: склады боеприпасов, топливные резервы, развёртывание полевых госпиталей, мостов и переправ | эск. | порядковая 0–3 | 2 |
| Д1.4 | Актуальные боевые действия: обстрелы, авиаудары, пограничные инциденты, попытки прорыва | эск. | счётная (CAP 5) | 1 верифиц. |
| Д1.5 | Развёртывание стратегических систем: ПВО, надводные группы, подводные силы, ядерная инфраструктура (без ядерной риторики — она в Д2) | эск. | порядковая 0–3 | 2 |
| Д1.6 | Подтверждённое разведение сил, вывод войск из позиций, сокращение активности | деэск. | порядковая 0–3 | 2 |

### Д2. Ядерная сигнальная активность (Nuclear signaling)

| # | Критерий | Направление | Шкала | Мин. источников |
|---|---|---|---|---|
| Д2.1 | Явные публичные угрозы применения ЯО на уровне глав государств, министров обороны/иностранных дел | эск. | порядковая 0–3 | 2 |
| Д2.2 | Изменение ядерной доктрины, выход из договоров по контролю над вооружениями, приостановка участия в механизмах уведомления | эск. | бинарная | 2 |
| Д2.3 | Подтверждённые технические сигналы: учения стратегических сил, подвижки ракетных комплексов, подготовка полигонов, смена режима дежурств | эск. | счётная (CAP 5) | 2 |
| Д2.4 | Публичное повышение готовности ядерных сил, заявления о переводе сил на особый режим | эск. | бинарная | 1 верифиц. |
| Д2.5 | Отказ от ядерной риторики, подтверждённое возвращение к штатному режиму сил, восстановление договорных механизмов | деэск. | бинарная | 2 |

### Д3. Дипломатическая эскалация / деэскалация (Diplomatic track)

| # | Критерий | Направление | Шкала | Мин. источников |
|---|---|---|---|---|
| Д3.1 | Разрыв или понижение дипломатических отношений, массовая высылка дипломатов, закрытие посольств | эск. | счётная (CAP 5) | 2 |
| Д3.2 | Ультиматумы и публично заявленные несовместимые требования сторонами конфликта | эск. | порядковая 0–3 | 2 |
| Д3.3 | Санкционные и международно-правовые решения: новые пакеты санкций, резолюции, признание конфликтующих сторон террористическими и т.п. | эск. | счётная (CAP 5) | 2 |
| Д3.4 | Возобновление или запуск переговоров, посредничество третьих стран и организаций | деэск. | порядковая 0–3 | 2 |
| Д3.5 | Договорённости о гуманитарных коридорах, обменах пленными, доступе инспекций | деэск. | счётная (CAP 5) | 1 верифиц. |

### Д4. Экономические индикаторы конфликта (Economic track)

| # | Критерий | Направление | Шкала | Мин. источников |
|---|---|---|---|---|
| Д4.1 | Новые пакеты санкций и контрсанкций, ограничения на экспорт энергоносителей и критичных товаров | эск. | счётная (CAP 5) | 2 |
| Д4.2 | Военные расходы: экстренные бюджетные ассигнования, переход экономики на военные рельсы (по открытым бюджетам и официальным решениям) | эск. | порядковая 0–3 | 2 |
| Д4.3 | Разрыв цепочек поставок критичных ресурсов: редкие металлы, микроэлектроника, компоненты ВПК | эск. | порядковая 0–3 | 2 |
| Д4.4 | Конфискации и национализации активов конфликтующих сторон, блокировка торговых путей | эск. | счётная (CAP 5) | 2 |
| Д4.5 | Отмена ограничений, восстановление торговых связей, гуманитарные экономические исключения | деэск. | счётная (CAP 5) | 2 |

### Д5. Информационная и киберсфера (Information & cyber)

| # | Критерий | Направление | Шкала | Мин. источников |
|---|---|---|---|---|
| Д5.1 | Эскалация военной риторики в официальных государственных СМИ и заявлениях: переход к языку неизбежности, дегуманизации противника | эск. | порядковая 0–3 | 2 |
| Д5.2 | Подтверждённые кибероперации против критической инфраструктуры (энергетика, связь, транспорт, госорганы) с атрибуцией конфликтующей стороной | эск. | счётная (CAP 5) | 2 |
| Д5.3 | Координированные дезинформационные кампании, разворачиваемые в преддверии военных действий (по отчётам проверенных аналитических центров) | эск. | порядковая 0–3 | 2 |
| Д5.4 | Снижение уровня военной пропаганды, закрытие каналов разжигания, публичные опровержения эскалационных нарративов | деэск. | порядковая 0–3 | 2 |

### Д6. Мобилизация общества (Mobilization)

| # | Критерий | Направление | Шкала | Мин. источников |
|---|---|---|---|---|
| Д6.1 | Введение или расширение военного положения, мобилизационные законы, скрытая мобилизация (по подтверждённым данным) | эск. | порядковая 0–3 | 2 |
| Д6.2 | Эвакуация населения из приграничных и прифронтовых зон, создание убежищ, учения гражданской обороны необычного масштаба | эск. | порядковая 0–3 | 2 |
| Д6.3 | Переориентация гражданской экономики: военные госзаказы, ограничение выезда для военнообязанных | эск. | бинарная | 2 |
| Д6.4 | Отмена мобилизационных мер, возвращение гражданских режимов, возвращение эвакуированных | деэск. | бинарная | 2 |

### Д7. Распространение и вовлечение третьих сторон (Spillover)

| # | Критерий | Направление | Шкала | Мин. источников |
|---|---|---|---|---|
| Д7.1 | Прямое военное вовлечение новых государств в конфликт (ввод войск, поставки оружия с эскалационным эффектом, базирование) | эск. | счётная (CAP 5) | 2 |
| Д7.2 | Атаки на объекты за пределами основного театра: торговое судно, трубопровод, дипмиссия, объекты инфраструктуры третьих стран | эск. | счётная (CAP 5) | 2 |
| Д7.3 | Открытие новых фронтов или театров в соседних регионах | эск. | бинарная | 1 верифиц. |
| Д7.4 | Выход третьих сторон из конфликта, прекращение поставок, закрытие базирования | деэск. | порядковая 0–3 | 2 |

### Д8. Подтверждённые деэскалирующие исходы (De-escalation outcomes)

Этот драйвер — не «отсутствие сигналов», а зафиксированные факты.
Отсутствие эскалации само по себе не снижает индекс: снижение требует
позитивного подтверждения (иначе индекс «провалился бы в ноль» в паузах
между активными фазами, что неверно отражает сохраняющееся напряжение).

Все критерии Д8 деэскалационные, поэтому драйвер не входит в сумму
эскалации напрямую: его сила `d₈` **вычитается** из сырого скора на этапе
глобальной агрегации (§5).

Дополнение v0.5 (критерий Д8.4): подтверждённые факты деэскалации
(подписанные договорённости) часто запаздывают относительно реального
снижения напряжения — «тихие» договорённости за закрытыми дверями неделями
не попадают в OSINT. Поэтому введён частичный сигнал по **тренду
интенсивности**: устойчивое снижение частоты эскалационных событий Д1–Д5
на протяжении трёх окон (не ноль, а устойчивый тренд вниз) даёт
деэскалационный сигнал порядковой шкалы, даже без подписанных бумаг. Это
не «отсутствие новостей», а измеренное изменение потока событий.

| # | Критерий | Направление | Шкала | Мин. источников |
|---|---|---|---|---|
| Д8.1 | Соблюдаемое прекращение огня / перемирие (не нарушенное в течение окна) | деэск. | бинарная | 2 |
| Д8.2 | Подписанные мирные договорённости, разведение сил и тяжёлого вооружения по верифицируемым схемам | деэск. | порядковая 0–3 | 2 |
| Д8.3 | Международное наблюдение и верификация: миссии, инспекции, мониторинг | деэск. | порядковая 0–3 | 1 верифиц. |
| Д8.4 | Устойчивое снижение интенсивности эскалационных событий Д1–Д5 на протяжении ≥ 3 окон без нарушения режимов | деэск. | порядковая 0–3 | 2 |

### Д9. Косвенные индикаторы подготовки (Shadow indicators)

**Обоснование.** История стратегического раннего предупреждения показывает:
подготовка к крупномасштабной войне почти всегда начинается **раньше и
тише**, чем видимые военные меры, — через медицину, финансы, транспорт,
статистику и быт. Эти сигналы слабее и зашумленнее прямых, но в совокупности
обладают высокой предвестниковой ценностью: их кластерное появление
исторически предшествовало активным фазам конфликтов. Поэтому они выделены
в отдельный драйвер со специальными правилами учёта (§4.3).

**Композитная структура (v0.4+).** Критерии объединены в **7 композитных
критериев-подгрупп**, каждый агрегирует свои подкритерии иерархически
(§4.3.7): сначала скор подгруппы, затем скор драйвера по подгруппам.
Подкритерии перечислены в описании как ориентир для аналитиков и не
являются самостоятельными единицами расчёта.

Критерии Д9 чувствительны к учениям, сезонности и «тренировкам готовности»,
поэтому **циклические** из них нормируются по **сезонной базовой линии**
(§4.3.3), а **ациклические** оцениваются по шкале без сезонной нормировки,
но с повышенным порогом короборации.

| # | Критерий (подгруппа) | Составляющие подкритерии | Направление | Шкала | Мин. источников | Тип |
|---|---|---|---|---|---|---|
| Д9.1 | Медицинская подготовка | Необычные госзакупки препаратов для лечения ранений (гемостатики, антибиотики широкого спектра, наркотические анальгетики, факторы свёртываемости), запасы крови и плазмы, расширение коечного фонда, ускоренная подготовка военных медиков | эск. | порядковая 0–3 | 2 | циклический |
| Д9.2 | Финансовая изоляция | Капитальные ограничения, отзыв активов из-за рубежа, приостановка выплат по внешнему долгу, военные займы, конвертация резервов, экспортные запреты на продовольствие и топливо | эск. | порядковая 0–3 | 2 | ациклический |
| Д9.3 | Транспорт и страхование | Скачок war risk premium, отзыв страхового покрытия, массовая отмена авиарейсов, закрытие воздушного пространства (NOTAM), уход судоходных компаний из портов, военные приоритеты на железных дорогах, изъятие подвижного состава, ремонт узлов и мостов в приграничье | эск. | порядковая 0–3 | 2 | циклический |
| Д9.4 | Стратегическое накопление и промышленная перестройка | Аномальные закупки в госрезервы (зерно, топливо, медикаменты, компоненты ВПК), круглосуточные режимы предприятий ВПК, мобилизационные заказы гражданским заводам, закупки станкостроительных линий, рост производства взрывчатки и пороха, островной режим энергосистем, накопление топлива вблизи театра | эск. | порядковая 0–3 | 2 | циклический |
| Д9.5 | Кадровые и мобилизационные меры | Изменение возраста и категорий призыва, расширение военнообязанных списков, всеобщие воинские учёта, ограничения выезда для отдельных категорий специалистов, изменения законов о военной службе без объявления мобилизации, ускоренный набор в кибервойска | эск. | порядковая 0–3 | 2 | ациклический |
| Д9.6 | Управление, связь и информационная подготовка | Усиление цензуры и фильтрации интернета, учения на автономность от глобальной сети, резервные каналы госуправления, перебазирование командных пунктов, «подогревка» госСМИ и военно-патриотические кампании, постановка помех GPS/Galileo, замена импортного ПО в критичных отраслях, выход из международных проектов, внезапное прекращение публикации статистики, перестановки в силовом блоке, вывоз семей дипломатов и чиновников, эвакуация музейных ценностей и архивов, закрытие посольств третьих стран | эск. | порядковая 0–3 | 2 | ациклический |
| Д9.7 | Снятие «тихих» мер | Отмена капконтроля, возвращение дипломатических семей, возобновление публикации статистики, возврат к гражданским режимам на предприятиях, возвращение эвакуированных коллекций, отмена учётных кампаний, возобновление обычных расписаний транспорта | деэск. | порядковая 0–3 | 2 | ациклический |

### 4.1. Общие правила учёта критериев

1. **Окно.** Учитываются только события со дня внутри текущего недельного
   окна. Событие старше окна влияет только через инерцию индекса (§5.4).
2. **Подтверждённость.** Каждый сигнал критерия проходит проверку
   подтверждения: минимум **2 независимых источника из разных кластеров
   происхождения** (§4.1.3) **либо 1 верифицированный primary/OSINT**.
   Жёсткий протокол верификации primary-источника: для фото/видео —
   геолокация и хронометраж, перекрёстная проверка теней, метеоусловий и
   ландшафта; для документов — проверка реестровой цепочки и
   независимого подтверждения фрагментов; для спутниковых данных —
   кросс-сенсорная сверка (разные аппараты/даты). Один верифицированный
   спутниковый снимок (Maxar/Planet) или первичный документ — полноценный
   сигнал, требование «второго источника» к ним не применяется.
3. **Независимость и генеалогия.** Источники не считаются независимыми,
   если имеют общего первоисточника (PRD §8.1, §19.8). **Генеалогия
   источника** прослеживается до первичного узла: если два СМИ ссылаются
   на один и тот же Telegram-канал или пул без собственной верификации —
   это один источник. Оценка независимости опирается на **кластеры
   происхождения** (решение открытого вопроса PRD §19.8):
   - государственные СМИ конфликтующих сторон;
   - мейнстрим-пул информагентств (Reuters/AP и прямые перепечатки);
   - открытые реестры и госзакупки (первичные документы);
   - спутниковый OSINT (Maxar, Planet и проверенные аналитики снимков);
   - полевой OSINT (геолоцированные фото/видео с первичными съёмками);
   - финансовые и рыночные данные (биржи, страховые индексы).
   Сигнал засчитывается как независимо подтверждённый, только если он
   опирается на источники из **разных кластеров**. Полная матрица
   происхождения (Provenance Matrix) с правилами засчитывания — приложение
   к версии методологии.
4. **Спекуляция.** Оценки без первичного подтверждения (анонимные источники
   без верификации, слухи, непроверенные соцсети) не входят в сигнал и не
   сдвигают уверенность — они попадают только в редакторский обзор (PRD §4.3).
5. **Одно событие — один сигнал; матрица пересечений.** Одно событие не
   может давать полный сигнал одновременно по нескольким критериям. Для
   каждого типа событий фиксируется **главный критерий**; вторичный
   критерий получает сигнал только при наличии **самостоятельного нового
   аспекта** события. Дерево решений (стартовые привязки, полная матрица —
   приложение к версии методологии):

   | Тип события | Главный критерий | Вторичный учёт |
   |---|---|---|
   | Официальный пакет санкций | Д3.3 | Д4.1 — только при появлении новых экономических последствий сверх самого решения |
   | Полевые госпитали и военная медлогистика | Д1.3 | Д9.1 — только гражданская закупка, не уже учтённая в Д1.3 |
   | Экстренные военные расходы | Д4.2 | Д9.4 — только структурная перестройка гражданской промышленности |
   | Эскалационная военная риторика | Д5.1 | Д9.6 — только устойчивая кампания смены тональности, а не отдельное заявление |

   Косвенные критерии Д9 дополнительно требуют, чтобы событие **не было
   уже учтено** в Д1–Д8: Д9 фиксирует только «гражданскую» оболочку сигнала.
6. **CAP.** Любой счётный критерий ограничен сверху (CAP 5): за одну неделю
   не более 5 единиц вклада. Это защищает от доминирования одного
   медийно-заметного события (PRD §2.3).
7. **Деэскалация только как факт.** Критерии с направлением `деэск.`
   принимают значение больше нуля только при наличии позитивного
   подтверждённого события; отсутствие эскалации им не засчитывается
   (исключение — измеренный тренд интенсивности, Д8.4).
8. **Региональная атрибуция.** Каждый сигнал критерия маппится на регион(ы)
   по единой таксономии (PRD §19.7). Сигнал без однозначной атрибуции
   идёт только в глобальный расчёт.

### 4.2. Сведение критериев в драйвер

Скор драйвера за неделю — **разность независимо нормированных эскалации
и деэскалации** (v0.5: устранены искажение знаменателя α и двусмысленность
знака, присущие форме v0.4):

```
sᵢ = clamp( Eᵢ / N_E,i − α · Bᵢ / N_B,i, 0, 1 )
```

где:

- **Eᵢ** — сумма эскалационных сигналов **покрытых** эскалационных
  критериев драйвера (после нормировки шкал: бинарная → 0/1; порядковая
  0–3 → 0, 0.33, 0.67, 1; счётная → n/CAP);
- **Bᵢ** — аналогичная сумма по покрытым деэскалационным критериям;
- **N_E,i** и **N_B,i** — максимально возможные суммы **только по покрытым
  критериям соответствующего направления**. Непокрытый критерий исключается
  из нормировки, а не засчитывается как ноль: иначе пробелы в данных
  систематически занижали бы скор. Эскалация и деэскалация нормируются
  каждая на свой знаменатель — α тогда задаёт чистую асимметрию силы
  сигналов, не искажая масштаб;
- **α** — коэффициент асимметрии деэскалации внутри смешанного драйвера
  (стартово 0.5: деэскалация снижает медленнее, чем эскалация поднимает —
  асимметрия напряжения; значение до калибровки). Если у драйвера нет
  покрытых критериев одного направления, соответствующий член равен 0.

**Порог покрытия.** Если доля покрытых критериев драйвера ниже **50 %**
(стартовый порог, до калибровки), драйвер получает `sᵢ = null` — это
фиксируется как слепая зона. Доля покрытия также отражается в уверенности
(§5.1), но не подменяет её.

**Драйвер Д8** (все критерии деэскалационные) считается отдельно как сила
деэскалации:

```
d₈ = clamp( E₈ / N₈_cov, 0, 1 )
```

где `N₈_cov` — максимум по покрытым критериям Д8 (включая Д8.4, чей сигнал
выводится из агрегированной интенсивности Д1–Д5, §8). С тем же правилом
покрытия: `d₈ = null` при покрытии ниже порога.

### 4.3. Специальные правила косвенных критериев (драйвер Д9)

Косвенные сигналы принципиально отличаются от прямых: они слабее,
зашумлены ложными аналогами (учения, сезонные закупки, рутинные учёты)
и исторически дают ложные срабатывания при отсутствии реальной подготовки.
Поэтому к Д9 применяются ограничительные правила:

1. **Пониженный потолок уверенности.** По умолчанию уверенность драйвера Д9
   не может быть «высокой»: потолок — «средняя», пока кластер косвенных
   сигналов не подтверждён хотя бы одним независимым прямым наблюдением
   из Д1–Д6.
2. **Короборация.** Косвенный сигнал получает полный вес только при
   подтверждении сигналами **разных подгрупп** (например, Д9.2 + Д9.4,
   Д9.3 + Д9.1). Кластер сигналов одной подгруппы взвешивается с
   коэффициентом 0.5. Для **ациклических** критериев (§4.3.3) порог
   повышен: требуется минимум **3 разные подгруппы**.
3. **Сезонная нормировка — только для циклических критериев.**
   - *Циклические* (Д9.1, Д9.3, Д9.4 — медицина, транспорт, закупки):
     сравниваются с базовой линией того же календарного периода за
     предыдущие 3 года (глубина — стартово, до калибровки); сигнал
     засчитывается только при превышении сезонной линии (защита от
     «осенних призывов» и «квартальных закупок»). При достаточном объёме
     исторических данных базовая линия считается робастной
     STL-декомпозицией вместо медианы; медиана остаётся fallback для рядов
     с короткой историей.
   - *Ациклические / политические* (Д9.2, Д9.5, Д9.6, Д9.7 — капконтроль,
     кадровые меры, сокрытие данных, снятие мер): не имеют сезонности,
     их «медиана за 3 года» равна нулю и нормировка бессмысленна.
     Они оцениваются по бинарной/порядковой шкале напрямую, без сезонной
     нормировки, но с повышенным порогом короборации (правило 2).
4. **Ограничение недельного прироста (лимит 30 %).** Вклад Д9 в недельный
   прирост индекса ограничен. Операциональное определение:
   ```
   ΔI_Д9 = min( ΔI_Д9_raw, 0.3 · (100 − I_{t−1}) )
   ```
   где `ΔI_Д9_raw` — вклад Д9 в прирост индекса за неделю до инерции,
   в пунктах шкалы 0–100. Прямые сигналы (Д1–Д7) этим лимитом не связаны.
5. **Абсолютный потолок вклада Д9 (v0.5).** Косвенные сигналы —
   предвестники, а не доказательства: без прямых действий они не должны
   доминировать в шкале риска, даже при многонедельном максимуме.
   Вклад Д9 в сырой скор ограничен долей **ρ = 25 %** (рабочий диапазон
   20–25 %, до калибровки):
   ```
   S = Σ_{i∈эск, i≠9} wᵢ'·sᵢ          (сумма прямых эскалационных вкладов)
   U = w₉'·s₉                          (вклад Д9)
   если U > (ρ / (1 − ρ)) · S, то U := (ρ / (1 − ρ)) · S
   ```
   После ограничения U/(S + U) ≤ ρ при любом наборе сигналов. Правила 4
   и 5 применяются совместно (ограничивают и скорость, и потолок).
6. **Публикация как наблюдение, не как факт.** В UI драйверы на основе Д9
   обязаны формулироваться как наблюдения («наблюдались необычные закупки…»),
   а не как установленные факты подготовки к войне (PRD §4.3, §2.3).
7. **Стартовый вес ниже, но не фиксирован.** w(Д9) стартует с 0.06 против
   0.1175 у прямых драйверов (сумма 1.0). Это стартовое значение, а не
   априорная константа: после бэктеста вес Д9 пересматривается с учётом
   потолка ρ (§4.3.5).
8. **Иерархическая агрегация.** Скор драйвера считается в два уровня:
   сначала скор каждой подгруппы по её подкритериям по формуле §4.2,
   затем скор Д9 по семи подгруппам с весами подгрупп (стартово равными,
   до факторного анализа). Это уменьшает эффективный знаменатель `N`
   и делает плотный кластер сигналов весомым, сохраняя защиту от
   одиночных ложных срабатываний (правила 1–3).

---

## 5. Формула глобального индекса

Пайплайн агрегации (v0.5):

```
1. sᵢ = clamp( Eᵢ/N_E,i − α·Bᵢ/N_B,i, 0, 1 )          — нетто-скор драйвера (§4.2)
2. d₈ = clamp( E₈ / N₈_cov, 0, 1 )                     — сила деэскалации (§4.2)
3. wᵢ' = (wᵢ · cᵢ) / Σⱼ (wⱼ · cⱼ)                      — веса с уверенностью (§5.1)
4. U = w₉'·s₉; если U > (ρ/(1−ρ))·S, то U := (ρ/(1−ρ))·S  — потолок Д9 (§4.3.5)
5. I_agg = max( 0, S + U − λ·w₈'·d₈ )                 — сырой скор с вычитанием деэскалации
6. I_internal = 100 · I_agg^k                         — степенное сжатие (§5.2)
7. I_published = round( итог с асимметричной инерцией, §5.4 )
   — округление только здесь
```

где:

- **sᵢ** — скор драйвера (§4.2). Если `sᵢ = null`, драйвер исключается из
  суммы, его вес перераспределяется пропорционально остальным покрытым
  драйверам; доля исключённых весов фиксируется как неполное покрытие.
- **wᵢ** — стартовый вес драйвера. Стартовое предложение до калибровки:
  w(Д1)…w(Д7) = 0.1175, w(Д8) = 0.1175, w(Д9) = 0.06 (PRD §4.3, спец.
  правило Д9; см. §4.3.7 о пересмотре). Веса документируются и меняются
  только через версионирование (PRD §8.5, §10).
- **cᵢ** — уверенность драйвера (§5.1) как **модификатор веса**: входит
  в перенормировку (шаг 3), а не умножается на сигнал. Низкая уверенность
  уменьшает влияние драйвера относительно других, но не искажает масштаб
  итоговой шкалы.
- **λ** — глобальный вес деэскалации (стартово **0.6**, рабочий диапазон
  0.5–0.7; до калибровки). Деэскалация через Д8 действительно снижает
  индекс.
- **k** — показатель степенного сжатия (§5.2).

### 5.1. Критерии уверенности драйвера

Уверенность снижается (PRD §8.1), если выполняется хотя бы одно условие:

1. объём данных меньше обычного за окно — значительная доля критериев
   драйвера не достигла минимального покрытия источников;
2. независимые сигналы противоречат друг другу — разные надёжные источники
   дают несовместимые оценки одного и того же факта;
3. покрытие источников неполное — есть слепые зоны по типам источников
   или регионам.

Уровни: нарушено одно условие → средняя; два и более → низкая; ни одного →
высокая (для Д9 — максимум средняя, см. §4.3.1). Помимо уровня в снапшоте
хранится **причина** («мало источников», «противоречия», «слепые зоны») и
**доля покрытия** критериев — это позволяет UI объяснять пользователю,
почему уверенность не высокая. Численная шкала cᵢ: высокая = 1.0,
средняя = 0.7, низкая = 0.4 (до калибровки). Независимость оценивается по
кластерам происхождения (§4.1.3): подтверждение источниками внутри одного
кластера не повышает уверенность.

### 5.2. Степенное сжатие

Вместо абстрактной «сигмоиды» (недоопределённость которой привела бы к
необходимости повторной нормировки выхода) применяется **степенная функция
сжатия** — математически строгий способ сделать верхнюю зону
труднодостижимой:

```
I_internal = 100 · I_agg^k,   I_agg ∈ [0, 1], k > 1
```

Свойства: при k = 1 — линейная шкала; при k > 1 — нижние значения
растягиваются, верхние сжимаются. Пример при k = 2: чтобы получить индекс
95, сырой скор должен быть √0.95 ≈ 0.975 — экстремальный набор
наблюдений, что и реализует принцип избегания ложной определённости
(PRD §2.3). Параметр **k** калибруется на бэктесте (стартово 1.5–2) так,
чтобы:

- при спокойном наборе сигналов индекс попадал в 0–20 (базовая линия
  перцентиля аномальности, §1.1);
- исторические пики-якоря (§9) попадали в 97–100 — иначе верхнее
  состояние недостижимо и critical mode никогда не активируется.

### 5.3. Региональная атрибуция в глобальном скоре

Глобальный индекс считается по всем сигналам независимо от атрибуции;
региональная детализация — производная (§7).

### 5.4. Инерция индекса и округление

Чтобы индекс не «проваливался» при тихой неделе сразу после тяжёлой,
вводится инерция. Внутреннее значение всегда хранится в float, **округление
только при публикации**:

```
рост:  I_internal,t = 0.9 · I_new + 0.1 · I_internal,t−1
снижение: I_internal,t = 0.5 · I_new + 0.5 · I_internal,t−1
I_published,t = round( I_internal,t )
```

где `I_new = 100 · I_agg^k` текущей недели. Инерция **асимметричная**
(v0.5): напряжение копится годами, а рассеивается месяцами — поэтому
индекс быстро реагирует на рост риска (90 % нового сигнала) и медленно —
на снижение (50 %). Это исторически и психологически обосновано и защищает
от циничной реакции «модель игнорирует мирный договор» после резкой
деэскалации. Стартовые коэффициенты 0.9 / 0.5 — до калибровки; симметричный
fallback β = 0.7 используется только до введения асимметрии в коде.
Триггер critical mode и пороги применяются к опубликованному
(инерционному) значению. Архив снапшотов хранит внутреннее неокруглённое
значение для воспроизводимости.

---

## 6. Недельное изменение и тренд

- **Изменение:** `Δ = I_t − I_{t−1}` — разность опубликованных снапшотов.
  Публикуется как `↑ +N` / `↓ −N` со своим цветом состояния (PRD §4.2.3).
- **Тренд:** скользящее окно сопоставимости — **12 недель** (PRD §4.4).
  Точки старше 12 недель в график не входят; полная история хранится
  отдельно в архиве снапшотов.
- Все 12 точек публикуются с датами и доступны интерактивно (tooltip).

---

## 7. Региональный индекс

Региональный индекс производный, но не копия глобального:

```
I_region = f( I_global, региональные_драйверы, экспозиция_региона )
```

Правила:

- **Региональные сигналы.** Каждый сигнал критерия, атрибутированный на
  регион (§4.1.8), считается в региональном скоре того же драйвера.
  Критерии Д2 (ядерная сфера) и Д4 (экономика) считаются в основном
  глобальными; их региональный эффект идёт через экспозицию, а не через
  дублирование сигнала. Д9 по умолчанию глобален: косвенные меры редко
  атрибутируются регионально, кроме Д9.3 (транспорт и страхование).
- **Экспозиция региона** — композит из двух разновременных частей:
  - **структурная** `e_struct` — альянсы, общие границы, торговые
    зависимости, базы, военные доктрины; меняется медленно,
    пересматривается не чаще раза в квартал;
  - **динамическая** `e_dyn` — доля региональных сигналов в общем потоке
    за окно и близость к активным событиям; обновляется еженедельно.
  - Итог: `e_r = γ·e_struct + (1 − γ)·e_dyn`, обе части нормированы
    в [0, 1], γ = 0.5 стартово (до калибровки).
- Формула: `I_region = round( I_global · (1 − e_r) + I_global^reg · e_r )`,
  где `I_global^reg` — гипотетический индекс, рассчитанный только по
  сигналам региона.
- **Зеркалирование глобального шока (v0.5).** Формула смешения выше
  искажает оценку нейтральных соседей конфликта: при высокой `e_r` и
  отсутствии собственных сигналов (`I_global^reg → 0`) регион «проваливается»
  ниже глобального уровня в разгар общемирового кризиса. Поэтому вводится
  правило: если глобальный индекс вырос резко (`Δ_global ≥ +15` за неделю,
  стартовый порог) и в регионе **нет подтверждённых деэскалационных
  сигналов**, то:
  ```
  I_region = max( I_region_formula, κ · I_global )
  ```
  где κ = 0.85 стартово (до калибровки) — корреляционный коэффициент
  «глобальный шок затрагивает всех». В мире нет региона, безразличного
  к общемировой эскалации.
- **Сжатие при малом объёме данных** (иерархическая байесовская логика):
  отклонение региона от глобального индекса сжимается пропорционально
  объёму региональных данных: чем меньше независимых сигналов по региону,
  тем ближе `I_region` к `I_global`. Для регионов с покрытием ниже
  порога региональный индекс **не вычисляется псевдоточно**: показывается
  глобальный индекс с пометкой «региональных данных недостаточно» (PRD §7).
- Индекс всегда **региональный**; город — только ярлык пользователя,
  маппинг «город → регион» идёт по единому справочнику (PRD §2.8, §5.7).
  Вероятность воздействия на конкретный город **не вычисляется**.
- Ranked-лист «где риск изменился сильнее всего» (PRD §4.6) — сортировка
  регионов по Δ_region за неделю.
- Уверенность публикуется по каждому региону в региональной таблице;
  регион с долей слепых зон выше порога публикуется с пониженной уверенностью
  или как «данные временно недоступны» (PRD §7).
- Если региональные данные недоступны, а глобальные доступны — показывается
  глобальный индекс с явной пометкой о недоступности региональных данных
  (PRD §7).

---

## 8. Дополнительные измерения риска

«Прямое военное столкновение» и «Риск применения ядерного оружия»
(PRD §4.5) — отдельные категориальные оценки: Низкий / Средний / Высокий.
Они не входят в число hero и не выражаются в процентах.

Правила маппинга на категории (стартово, до калибровки):

| Категория | Условие |
|---|---|
| Низкий | Нет сигналов по профильным критериям за окно (Д1.4 для столкновения; Д2 для ядерного риска) |
| Средний | Есть подтверждённые сигналы, но без новых эскалационных и без нарушения режимов |
| Высокий | Новые эскалационные сигналы за окно либо подтверждённое нарушение прекращения огня / штатных режимов |

Точная оценка (численная) раскрывается только при соблюдении
трёх условий:

1. указан горизонт (например, 12 месяцев);
2. формально определено событие, вероятность которого оценивается;
3. приведена оговорка о калибровке.

Эти оценки — самостоятельная модель с собственной валидацией, подчинённая
тому же недельному циклу снапшотов и версионированию.

---

## 9. Состояния и пороги

Индекс маппится в шесть состояний (PRD §10):

| Диапазон | RU | EN |
|---:|---|---|
| 0–20 | Спокойно | Calm |
| 21–40 | Напряжённо | Tense |
| 41–60 | Опасно | Dangerous |
| 61–80 | Очень опасно | Very dangerous |
| 81–96 | Критически опасно | Critically dangerous |
| 97–100 | Экстремальная угроза | Extreme threat |

Правила порогов:

- **Исторические якоря (решение открытого вопроса PRD §19.3).** Пороги
  калибруются не абстрактной математикой, а привязкой к известным
  событиям. Бэктест обязан помещать якоря в заявленные кластеры:

  | Якорь | Целевой диапазон |
  |---|---|
  | Рутина 2010-х гг. | 0–20 |
  | Санкционные войны, локальные прокси-конфликты без прямого столкновения держав | 21–40 |
  | Крупные локальные войны с вовлечением ядерных держав (косвенным), разрыв дипотношений | 41–60 |
  | Прямые столкновения конвенциональных сил крупных блоков (Йом-Кипур 1973) | 61–80 |
  | Карибский кризис (1962), преддверие вторжения в Ирак (2003) | 81–100 |

  Подбор k, весов и λ (§5) итерируется до выполнения якорных попаданий;
  фиксация — в журнале калибровки версии.
- Каждый порог требует **документированного обоснования** на исторических
  данных (бэктест). PRD не подаёт числа как самоочевидную истину (§10).
- Калибровка степенного сжатия (§5.2) обязана обеспечивать достижимость
  диапазона 97–100 историческими пиками; недостижимое верхнее состояние
  подрывает практическую ценность шкалы.
- **Резервный триггер critical mode.** Даже если индекс ниже 97, critical
  mode активируется правилом-страховкой: `s_Д1 ≥ 0.67` И `s_Д2 ≥ 0.67`
  И `Δ ≥ +10` за неделю (стартовые значения, до калибровки). Это защищает
  от ситуации, когда экстремальная комбинация сигналов недостаточно
  отражена степенным сжатием.
- Изменение порогов — только через версионирование методологии и ревью.
- Для защиты от мерцания статуса на границе диапазона вводится
  **гистерезис**: переход в более опасное состояние происходит на верхней
  границе диапазона, обратный переход — на границе, смещённой на 2 пункта
  вниз (значение гистерезиса — до калибровки).
- Состояние «Экстремальная угроза» дополнительно активирует critical mode
  (PRD §6) — коммуникационное состояние интерфейса, а не объявление ЧС.

---

## 10. Версионирование и сопоставимость

Каждый снапшот хранит версию методологии/модели (PRD §8.5), а также
(для воспроизводимости расчёта):

- версию кода расчётного пайплайна;
- снапшот всех численных параметров (веса, α, β↑/β↓, λ, k, γ, κ, ρ,
  шкала cᵢ, пороги покрытия);
- ссылку/хэш набора входных данных.

Изменение считается **ломающим сопоставимость**, если меняется хотя бы
одно из:

- набор критериев или драйверов, их веса;
- формула агрегации, нормировка шкал, значения CAP;
- коэффициенты (α, β↑/β↓, λ, k, γ, κ, ρ), шкала уверенности, пороги
  покрытия;
- спец. правила Д9 (потолок уверенности, короборация, лимит 30 % и
  потолок ρ, иерархическая агрегация, сезонная линия, типизация
  циклических/ациклических);
- пороги состояний, правила critical mode, ширина гистерезиса,
  якорная таблица;
- модель региональной экспозиции (структура e_struct / e_dyn, γ, правило
  зеркалирования κ);
- кластеры происхождения источников и правила Provenance Matrix;
- правила Flash Alert.

При ломающем изменении:

1. история не пересчитывается молча;
2. решение о пересчёте истории принимается отдельно, с фиксацией
   аудиторского следа (кто, когда, почему изменил снапшот — PRD §19.9);
3. UI раскрывает несопоставимость рядом с затронутыми данными.

Параметры подлежат **регулярной перекалибровке** (не реже раза в год и
после каждого значимого исторического события) с тем же аудиторским следом.

---

## 11. Состояния данных и отказ

Явные состояния публикации (PRD §7): Published, Updating, Delayed,
Insufficient data, Model unavailable, Historical snapshot, **Flash Alert**
(§2.1 — внеплановый алерт, не изменяющий снапшот).

Правила:

- Устаревшее значение не показывается как текущее без явного индикатора
  возраста и даты.
- **Insufficient data:** если доля драйверов с `sᵢ = null` (слепыми зонами)
  превышает порог публикации (порог — открытый вопрос PRD §19.4), снапшот
  либо не публикуется, либо публикуется со сниженной уверенностью по
  затронутым драйверам/регионам — решение фиксируется в версии методологии.
- **Model unavailable:** показывается последний валидный снапшот с явной
  пометкой «Historical snapshot».

---

## 12. Ограничения модели (публичная часть)

Методология обязана публично описывать (PRD §8.3):

- что модель измеряет — состояние риска по подтверждённым открытым сигналам
  (включая косвенные индикаторы подготовки), выраженное как перцентиль
  аномальности относительно базовой линии (§1.1);
- что не измеряет — вероятность войны, риск удара по конкретному городу,
  дату события, сам факт тайной подготовки (косвенные сигналы — это
  наблюдения, а не доказательства);
- пробелы в данных — неравномерное покрытие источников по регионам
  и типам сигналов; слабая доступность данных для Д9 в закрытых странах;
  неизбежное влияние покрытия на уверенность и, при малом объёме, на
  региональный индекс (сжатие к глобальному, §7);
- асимметрия наблюдаемости деэскалации — подписанные договорённости
  запаздывают относительно реального снижения напряжения; критерий Д8.4
  частично компенсирует это, но закрытые переговоры принципиально
  не видны OSINT до публикации;
- конфликты источников — как противоречия влияют на уверенность;
- случаи отказа — когда снапшот не публикуется;
- известные ошибки первого и второго рода (ложные срабатывания и пропуски)
  на исторических данных, включая зафиксированные ложные кластеры Д9
  (PRD §8.4).

Историческая производительность публикуется в разборе «где ошиблись / где
были правы / где была неопределённость» (PRD §8.4).

---

## 13. Открытые вопросы методологии

Снято в v0.5: интерпретация шкалы (§1.1, было PRD §19.1), поведение между
релизами (§2.1, было §19.5), обоснование порогов через исторические якоря
(§9, было §19.3), критерий независимости источников через кластеры
происхождения (§4.1.3, было §19.8).

Остаются открытыми (снять до продакшена, трекинг — PRD §19):

1. **Бэктест и калибровка**: k, веса (включая обоснование веса Д9 с учётом
   ρ), α, λ, γ, κ, β↑/β↓, шкала cᵢ, пороги покрытия, пороги состояний,
   резервный триггер critical mode, ширина гистерезиса — подбор итерируется
   до выполнения якорных попаданий (PRD §19.2).
2. Минимальное покрытие источников для публикации: порог доли драйверов
   с `sᵢ = null` (PRD §19.4).
3. Аудиторский след изменённого исторического снапшота (PRD §19.9).
4. Региональная таксономия и справочник «город ↔ регион» (PRD §19.7, §19.13).
5. Сезонная базовая линия для циклических критериев Д9: глубина истории
   (3 года — стартово), обработка стран без статистики, переход на STL
   при достаточном объёме.
6. Пороги короборации Д9: коэффициент 0.5 для одной подгруппы и
   требование 3 подгрупп для ациклических критериев; веса подгрупп в
   иерархической агрегации (стартово равные).
7. Лимит 30 % и потолок ρ = 25 % для Д9: обоснование долей на исторических
   данных.
8. Полная матрица пересечений критериев (§4.1.5) и полная Provenance
   Matrix (§4.1.3) — расширить стартовые привязки до полных справочников.
9. Пороги зеркалирования регионального шока: Δ ≥ +15 и κ = 0.85.
10. Калибровка асимметричной инерции β↑/β↓ и её валидация на исторических
    эпизодах быстрой деэскалации.

---

## Приложение А. История версий

| Версия | Изменения |
|---|---|
| 0.1–0.2 | Первоначальный каркас (PRD v1.3) |
| 0.3 | Добавлены косвенные критерии подготовки (Д9, 17 критериев) |
| 0.4 | Исправления по результатам внешнего ревью: знак деэскалации и роль Д8 (вычитание через λ), явная сигмоида σ с калибруемым k, уверенность как модификатор веса (перенормировка), скор по покрытым критериям с порогом покрытия 50 %, Д9 реструктурирован до 7 композитных критериев с иерархической агрегацией, операционализирован лимит 30 %, региональная экспозиция разделена на структурную/динамическую со сжатием при малом объёме данных, матрица пересечений критериев, отказ от промежуточного округления, резервный триггер critical mode, хранение версий кода и параметров в снапшоте |
| 0.5 | Второй раунд ревью: формула скора как разность независимых нормировок Eᵢ/N_E,i − α·Bᵢ/N_B,i (устранено искажение знаменателя α), степенное сжатие I^k вместо сигмоиды, правило зеркалирования глобального шока в регионах (κ), «2 независимых ИЛИ 1 верифицированный primary/OSINT» с протоколом верификации, генеалогия источников и кластеры происхождения (Provenance Matrix), абсолютный потолок вклада Д9 ρ = 25 %, типизация критериев Д9 на циклические/ациклические, критерий Д8.4 (тренд интенсивности), перцентильная интерпретация шкалы, Flash Alert, исторические якоря порогов, асимметричная инерция (β↑ = 0.9, β↓ = 0.5) |
