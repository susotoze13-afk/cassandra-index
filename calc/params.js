// params.js — снапшот численных параметров версии методологии (§10).
// Все значения — стартовые, до калибровки (§10.1); меняются только через
// версионирование. k — единственный калибруемый здесь параметр: таск 03
// подбирает его на якорях §9 и фиксирует в журнале калибровки.

// Стартовое k = 2.0 → калибровано до k = 1.95 прогоном таска 03
// (node calc/calibrate.js, якорные профили §9, rolling-origin; аудиторский
// след и таблица чувствительности ±20 % — docs/calibration-journal.md,
// Autopilot-прогон 2026-09-21). С k = 1.95 якоря дают: рутина 2010-х ≈ 8
// (0–20), санкционные/прокси ≈ 32 (21–40), Крым-2014 ≈ 50 (41–60),
// Грузия-2008 ≈ 69 (61–80), февраль-2022 ≈ 91 (81–96, проверка),
// Карибский-1962 ≈ 99 (97–100, проверка). Максимум индекса ограничен
// ρ/U_abs и лимитом прироста Д9 (§4.3.4), поэтому 97–100 достижим только
// экстремальным профилем Карибского кризиса.
export const PARAMS = {
  version: '1.0',
  // Веса драйверов §5: Д1–Д8 по 0.1175, Д9 — 0.06 (сумма 1.0).
  weights: {
    D1: 0.1175, D2: 0.1175, D3: 0.1175, D4: 0.1175, D5: 0.1175,
    D6: 0.1175, D7: 0.1175, D8: 0.1175, D9: 0.06,
  },
  alpha: 0.5,        // α — асимметрия деэскалации внутри смешанного драйвера (§4.2)
  lambda: 0.6,       // λ — глобальный вес деэскалации Д8 (§5, шаг 4)
  rho: 0.25,         // ρ — предельная доля Д9 в сыром скоре (§4.3.5)
  U_abs: 0.025,      // малый абсолютный потолок Д9 при S = 0 (§4.3.5)
  gamma: 0.5,        // γ — доля структурной экспозиции региона (§7)
  kappa: 0.85,       // κ — коэффициент зеркалирования глобального шока (§7)
  k: 1.95,         // параметр насыщающей кривой (§5.2) — калиброван на якорях §9 (см. журнал выше)
  betaUp: 0.9,       // β↑ — инерция на рост (§5.4)
  betaDown: 0.5,     // β↓ — инерция на снижение (§5.4)
  // Structural Break Override: переопределение β на снижение на 2 недели (§5.4)
  structuralBreak: { beta: 0.8, prev: 0.2, weeks: 2 },
  n0: 5,             // n₀ — объёмное сжатие регионального отклонения (§7)
  regionClamp: 40,   // clamp Δ_region ±40 (§7)
  regionShockDelta: 15, // порог Δ_global для зеркалирования шока (§7)
  coverageThreshold: 0.5, // порог покрытия критериев драйвера (§4.2)
  d9GrowthLimit: 0.3, // лимит недельного прироста индекса от Д9 (§4.3.4)
  confidence: { high: 1.0, medium: 0.7, low: 0.4 }, // шкала cᵢ (§5.1)
  // --- Поля v0.7 (breaking-параметры — фиксируются только версионированием §10) ---
  capCount: 5,        // CAP — нормировка счётных критериев Σsev/CAP (§7.3); breaking
  severityValues: [0.2, 0.5, 1.0, 2.0], // допустимые веса событий (§7.3); breaking
  // Пороги покрытия для классификации публикации (R11–R15): nullWeight > 0.4
  // или null Д1/Д2 → insufficient; 0.2–0.4 → reduced; < 0.2 → полная.
  dataCoverage: { insufficient: 0.4, reduced: 0.2, d1d2Required: true }, // breaking
  // Whitelist кластеров происхождения источников A–F (R21–R23); breaking
  clusters: [
    'A-mainstream', 'B-state-media', 'C-registries',
    'D-satellite-osint', 'E-field-osint', 'F-financial',
  ],
  // Flash-триггеры (R16–R19): событие severity 2.0 по Д1.4/Д2.4 или Д7.3 = 1;
  // breaking
  flashTriggers: { criteria: ['D1.4', 'D2.4'], severity: 2.0, d7Value: 1 },
  // Пороги бейджа качества данных (R59): q ≥ 0.8 высокое, 0.6–0.8 среднее,
  // < 0.6 низкое; breaking
  qualityThresholds: { high: 0.8, medium: 0.6 },
  // Лестница IP-точности, % (R38, R48): cityConf ≥ 80 → город, ≥ 50 → админцентр,
  // < 50 → без города; regionConf ≥ 90 → регион, иначе только страна; любая ось
  // < floor → глобальный вид; breaking
  ipConfidenceLadder: { city: 80, region: 90, floor: 50 },
  // Пороги состояний §9 (применяются к опубликованному значению).
  states: [
    { min: 0, max: 20, id: 'calm' },
    { min: 21, max: 40, id: 'tense' },
    { min: 41, max: 60, id: 'danger' },
    { min: 61, max: 80, id: 'very' },
    { min: 81, max: 96, id: 'critical' },
    { min: 97, max: 100, id: 'extreme' },
  ],
};

// Глубокая копия снапшота — вызывающий не может мутировать PARAMS.
export function list() {
  return JSON.parse(JSON.stringify(PARAMS));
}
